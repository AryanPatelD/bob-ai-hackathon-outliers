# Live Demo URL

GridWise AI runs locally — it requires Python dependencies and generates synthetic data at startup.

To run locally:
  uvicorn src.backend.main:app --host 0.0.0.0 --port 8000 --reload

Then open: http://localhost:8000/dashboard

Cloud deployment: Not configured for this hackathon submission.
If deployed, this file would contain the URL.
